# 🎯 Guía de Referencia Rápida\n\nReferencia rápida para tareas comunes en Restaurant App.\n\n## Crear un Componente Nuevo\n\n```typescript\n// src/components/mi-componente/MiComponente.tsx\nimport { FC } from 'react';\nimport { Dialog, TextField, Button } from '@mui/material';\nimport { MiComponenteProps } from './mi-componente.types';\n\nexport const MiComponente: FC<MiComponenteProps> = ({\n  open,\n  onClose,\n  onSuccess\n}) => {\n  const [loading, setLoading] = useState(false);\n  const [formData, setFormData] = useState({});\n\n  const handleSubmit = async () => {\n    setLoading(true);\n    try {\n      // Lógica aquí\n      onSuccess?.();\n      onClose();\n    } catch (error) {\n      console.error(error);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  return (\n    <Dialog open={open} onClose={onClose}>\n      {/* Contenido */}\n    </Dialog>\n  );\n};\n```\n\n## Crear un Servicio Nuevo\n\n```typescript\n// src/services/mi-dato.service.ts\nimport { api } from './api';\nimport { MiDato, CreateMiDatoDTO } from '@/types/mi-dato.types';\n\nexport const miDatoService = {\n  async getAll() {\n    const { data } = await api.get('/mi-dato');\n    return data as MiDato[];\n  },\n\n  async getById(id: number) {\n    const { data } = await api.get(`/mi-dato/${id}`);\n    return data as MiDato;\n  },\n\n  async create(dto: CreateMiDatoDTO) {\n    const { data } = await api.post('/mi-dato', dto);\n    return data as MiDato;\n  },\n\n  async update(id: number, dto: Partial<CreateMiDatoDTO>) {\n    const { data } = await api.put(`/mi-dato/${id}`, dto);\n    return data as MiDato;\n  },\n\n  async delete(id: number) {\n    await api.delete(`/mi-dato/${id}`);\n  }\n};\n```\n\n## Usar Datos con Loading y Error\n\n```typescript\nconst MyPage = () => {\n  const [data, setData] = useState<MiDato[]>([]);\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  useEffect(() => {\n    const loadData = async () => {\n      setLoading(true);\n      try {\n        const result = await miDatoService.getAll();\n        setData(result);\n      } catch (err) {\n        setError(getApiErrorMessage(err));\n      } finally {\n        setLoading(false);\n      }\n    };\n\n    loadData();\n  }, []);\n\n  if (loading) return <CircularProgress />;\n  if (error) return <Alert severity=\"error\">{error}</Alert>;\n  if (!data.length) return <Typography>No hay datos</Typography>;\n\n  return <List>{/* Renderizar data */}</List>;\n};\n```\n\n## Mostrar Notificación\n\n```typescript\nconst MyComponent = () => {\n  const { showSnackbar } = useUi();\n\n  const handleSuccess = () => {\n    showSnackbar('Operación exitosa', 'success');\n  };\n\n  const handleError = () => {\n    showSnackbar('Algo salió mal', 'error');\n  };\n\n  return (\n    <div>\n      <button onClick={handleSuccess}>Success</button>\n      <button onClick={handleError}>Error</button>\n    </div>\n  );\n};\n```\n\n## Confirmación de Acción\n\n```typescript\nconst handleDelete = (id: number) => {\n  const { openDialog } = useUi();\n\n  openDialog({\n    id: `delete-${id}`,\n    title: 'Confirmar Eliminación',\n    message: '¿Estás seguro?',\n    onConfirm: async () => {\n      try {\n        await service.delete(id);\n        showSnackbar('Eliminado', 'success');\n      } catch (error) {\n        showSnackbar(getApiErrorMessage(error), 'error');\n      }\n    }\n  });\n};\n```\n\n## Validar Rol de Usuario\n\n```typescript\nconst AdminPanel = () => {\n  const { user, hasRole } = useAuth();\n\n  if (!hasRole('admin')) {\n    return <Redirect to=\"/forbidden\" />;\n  }\n\n  return <div>Panel Admin</div>;\n};\n```\n\n## Navegar a Otra Página\n\n```typescript\nconst MyComponent = () => {\n  const navigate = useNavigate();\n\n  const handleClick = () => {\n    navigate('/admin/productos');\n    // O con parámetro\n    navigate(`/admin/productos/${id}`);\n    // O con estado\n    navigate('/dashboard', { state: { tab: 'analytics' } });\n  };\n\n  return <button onClick={handleClick}>Ir a Productos</button>;\n};\n```\n\n## Usar Parámetros de Ruta\n\n```typescript\nconst ProductDetail = () => {\n  const { id } = useParams<{ id: string }>();\n  const [product, setProduct] = useState<Product | null>(null);\n\n  useEffect(() => {\n    productosService.getProductById(Number(id)).then(setProduct);\n  }, [id]);\n\n  return <div>{product?.nombre}</div>;\n};\n```\n\n## Tabla con CRUD\n\n```typescript\nconst ProductsTable = () => {\n  const [products, setProducts] = useState<Product[]>([]);\n  const [editingId, setEditingId] = useState<number | null>(null);\n  const [openDialog, setOpenDialog] = useState(false);\n\n  const handleEdit = (id: number) => {\n    setEditingId(id);\n    setOpenDialog(true);\n  };\n\n  const handleSuccess = async () => {\n    const updated = await productosService.getAll();\n    setProducts(updated);\n    setOpenDialog(false);\n  };\n\n  const handleDelete = async (id: number) => {\n    if (confirm('¿Eliminar?')) {\n      await productosService.delete(id);\n      setProducts(products.filter(p => p.id !== id));\n    }\n  };\n\n  return (\n    <>\n      <TableContainer>\n        <Table>\n          <TableBody>\n            {products.map(product => (\n              <TableRow key={product.id}>\n                <TableCell>{product.nombre}</TableCell>\n                <TableCell>\n                  <Button onClick={() => handleEdit(product.id)}>Editar</Button>\n                  <Button onClick={() => handleDelete(product.id)}>Eliminar</Button>\n                </TableCell>\n              </TableRow>\n            ))}\n          </TableBody>\n        </Table>\n      </TableContainer>\n\n      <ProductFormDialog\n        open={openDialog}\n        onClose={() => setOpenDialog(false)}\n        onSuccess={handleSuccess}\n        productoId={editingId || undefined}\n      />\n    </>\n  );\n};\n```\n\n## Campos de Formulario Comunes\n\n```typescript\n// TextField de texto\n<TextField\n  label=\"Nombre\"\n  value={formData.nombre}\n  onChange={(e) => setFormData({ ...formData, nombre: e.target.value })}\n  required\n/>\n\n// Select/Dropdown\n<Select\n  value={formData.categoryId}\n  onChange={(e) => setFormData({ ...formData, categoryId: e.target.value })}\n>\n  {categories.map(cat => (\n    <MenuItem key={cat.id} value={cat.id}>{cat.nombre}</MenuItem>\n  ))}\n</Select>\n\n// Checkbox\n<FormControlLabel\n  control={<Checkbox\n    checked={formData.active}\n    onChange={(e) => setFormData({ ...formData, active: e.target.checked })}\n  />}\n  label=\"Activo\"\n/>\n\n// TextField de número\n<TextField\n  type=\"number\"\n  label=\"Precio\"\n  value={formData.price}\n  onChange={(e) => setFormData({ ...formData, price: parseFloat(e.target.value) })}\n/>\n\n// TextField de fecha\n<TextField\n  type=\"date\"\n  label=\"Fecha\"\n  value={formData.date}\n  onChange={(e) => setFormData({ ...formData, date: e.target.value })}\n  InputLabelProps={{ shrink: true }}\n/>\n```\n\n## Estilos con Material-UI\n\n```typescript\n<Box\n  sx={{\n    display: 'flex',\n    flexDirection: 'column',\n    gap: 2,\n    padding: 2,\n    backgroundColor: '#f5f5f5',\n    borderRadius: 1\n  }}\n>\n  {/* Contenido */}\n</Box>\n\n<Button\n  variant=\"contained\"\n  color=\"primary\"\n  size=\"small\"\n  disabled={loading}\n  fullWidth\n>\n  Click\n</Button>\n```\n\n## Variables de Entorno\n\n```env\n# .env\nVITE_API_BASE_URL=http://localhost:3000/api\nVITE_APP_NAME=Restaurant App\n```\n\n```typescript\n// Usar en código\nconst apiUrl = import.meta.env.VITE_API_BASE_URL;\nconst appName = import.meta.env.VITE_APP_NAME;\n```\n\n## Comandos Útiles\n\n```bash\n# Desarrollo\nnpm run dev              # Inicia servidor\nnpm run build            # Compilar producción\nnpm run lint             # Revisar errores\nnpm test                 # Ejecutar pruebas\nnpm run test:watch       # Pruebas en watch\n```\n\n## Debug en Navegador\n\n```typescript\n// Pausar ejecución\nif (import.meta.env.DEV) {\n  console.log('Debug:', data);\n  debugger; // Se pausará aquí\n}\n\n// Logging condicional\nif (import.meta.env.DEV) {\n  console.log('Estado:', state);\n}\n```\n\n## Tipos TypeScript Útiles\n\n```typescript\n// Función que retorna Promise\nconst getData = async (): Promise<Data[]> => {\n  return [];\n};\n\n// Tipo Union\ntype Status = 'active' | 'inactive' | 'pending';\n\n// Tipo Partial\nconst updateUser = (id: number, data: Partial<User>) => {\n  // data puede no tener todos los campos\n};\n\n// Tipo Pick\ntype UserPreview = Pick<User, 'id' | 'nombre'>;\n\n// Tipo Omit\ntype UserWithoutPassword = Omit<User, 'password'>;\n```\n\n---\n\nPara más detalles, consulta la documentación principal en [docs/README.md](README.md)\n